# dependencies
node_modules/
.pnpm-store/

# builds
.next/
dist/
out/
build/
.turbo/

# env — only examples are committed
.env
.env.*
!.env.example

# logs & misc
*.log
.DS_Store
coverage/
*.tsbuildinfo

# prisma
apps/api/prisma/*.db

# local volumes
docker/volumes/

# prisma generated client (recreated via pnpm db:generate)
apps/api/src/generated/

# uploaded files (local disk storage adapter)
apps/api/uploads/

# Pre-wipe dumps from prisma/scripts/backup-org.ts. Real people, real bank
# details — a file that must never be pushed to a public remote.
org-backup*.json
